# Copy this file to .env and fill in your values.

# ── Resource limits per user container ──
MEM_LIMIT=512M
CPU_LIMIT=1.0

# ── Docker Compose project name ──
COMPOSE_PROJECT_NAME=rlm-jupyter

# ── LLM API Key (set ONE of the following) ──
# The key you set here is passed into every user's notebook container
# so the tutorial can call the LLM backend.

# Option 1: OpenAI
# OPENAI_API_KEY=sk-...
# OPENAI_MODEL=gpt-4o-mini

# Option 2: Anthropic
# ANTHROPIC_API_KEY=sk-ant-...
# ANTHROPIC_MODEL=claude-sonnet-4-20250514

# Option 3: Portkey (router for multiple providers)
# PORTKEY_API_KEY=...
# PORTKEY_MODEL=@openai/gpt-4o-mini

# Option 4: OpenRouter
# OPENROUTER_API_KEY=...

# Option 5: Local vLLM
# VLLM_BASE_URL=http://host.docker.internal:8080
